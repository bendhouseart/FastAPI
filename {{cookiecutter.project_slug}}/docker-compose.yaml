version: "3"
services:
  web:
    build:
      context: .
      dockerfile: app.dockerfile
    ports:
      - "8000:8000"
  db:
    image: postgres
    env_file:
      - database.env
    volumes:
    - database-data:/var/lib/postgresql/data

volumes:
  database-data: